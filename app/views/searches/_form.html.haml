= form_for @search, remote: true, html: { class: 'form-horizontal' } do |f|
  .control-group
    = f.label :city, class: 'control-label'
    .controls
      = f.text_field :city, class: 'text_field'
      = f.submit "Search", class: 'btn btn-primary'
   